name: amt-deltaker-bff - Deploy app
on:
  workflow_dispatch: # This workflow can be triggered manually
  push:
    paths:
      - "amt-deltaker-bff/**"
      - "gradle/libs.versions.toml"
    branches:
      - main

permissions:
  id-token: write

jobs:
  call-deploy:
    uses: ./.github/workflows/reusable/deploy-app.yml
    with:
      module-path: "amt-deltaker-bff"