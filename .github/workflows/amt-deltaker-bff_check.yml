name: amt-deltaker-bff - Run tests
on:
  push:
    paths:
      - "amt-deltaker-bff/**"
    branches-ignore:
      - main

permissions:
  contents: read

jobs:
  run-tests:
    uses: ./.github/workflows/test.yml
    with:
      module-path: "amt-deltaker-bff"