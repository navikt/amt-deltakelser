name: amt-lib - Run tests
on:
  push:
    paths:
      - "amt-lib/**"
      - ".github/dependabot.yml"
    branches:
      - main

permissions:
  contents: "write"

jobs:
  run-tests:
    uses: ./.github/workflows/test.yml
    with:
      module-path: "amt-lib"