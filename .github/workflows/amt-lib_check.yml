name: amt-lib - Run tests
on:
  push:
    paths:
      - "amt-lib/**"
    branches-ignore:
      - main

permissions:
  contents: read

jobs:
  run-tests:
    uses: ./.github/workflows/test.yml
    with:
      module-path: "amt-lib"