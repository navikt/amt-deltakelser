name: amt-lib - Run tests
on:
  push:
    paths:
      - "amt-lib/**"
      - "gradle/libs.versions.toml"
    branches-ignore:
      - main

permissions:
  contents: "write"

jobs:
  run-tests:
    uses: ./.github/workflows/test.yml
    with:
      module-path: "amt-lib"