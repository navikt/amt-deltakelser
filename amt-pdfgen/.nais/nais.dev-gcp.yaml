---
spec:
  replicas:
    disableAutoScaling: true
    min: 1
    max: 1
  resources:
    requests:
      memory: 256Mi
    limits:
      memory: 1024Mi
  env:
    - name: JDK_JAVA_OPTIONS
      value: "-XX:MaxRAMPercentage=60.0"
